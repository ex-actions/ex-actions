version: '3'

services:
  app:
    build:
      context: './'
      dockerfile: Dockerfile
    tty: true
    volumes:
      - ./test_apps:/test_apps
      - /test_apps/app/deps
      - /test_apps/app/_build
